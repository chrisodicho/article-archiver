#!/usr/bin/env node

try {
  require = require('esm')(module /*, options*/);
  require('../lib/cli');
} catch (error) {
  console.error('article-archiver failed to run. Try again with the --debug flag.');
  console.error(error);
}
